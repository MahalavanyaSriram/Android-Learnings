<?xml version="1.0" encoding="utf-8"?>
<!-- 
 a. Assignment : #07.
 b. File Name : AndroidManifest.xml (app/src/main/AndroidManifest.xml).
 c. Full name of the student : Mahalavanya Sriram, Chandan Mannem.
 -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mad.contacts">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_contacts_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Contacts">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>